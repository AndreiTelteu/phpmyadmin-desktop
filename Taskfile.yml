version: '3'

vars:
  APP_NAME: phpmyadmin-desktop
  BIN_DIR: bin

tasks:
  frontend:
    desc: Build the Solid frontend for embedding
    dir: frontend
    cmds:
      - npm install
      - npm run build

  bindings:
    desc: Regenerate Wails v3 TypeScript bindings
    cmds:
      - wails3 generate bindings -clean -b -names -ts -d frontend/bindings

  build:
    desc: Build the desktop app for the current platform
    deps: [frontend, bindings]
    cmds:
      - mkdir -p {{.BIN_DIR}}
      - go build -o {{.BIN_DIR}}/{{.APP_NAME}}

  build-windows:
    desc: Build the production Windows GUI executable without a console window
    deps: [frontend, bindings]
    cmds:
      - mkdir -p {{.BIN_DIR}}
      - GOOS=windows GOARCH=amd64 go build -tags production -trimpath -buildvcs=false -ldflags="-w -s -H windowsgui" -o {{.BIN_DIR}}/{{.APP_NAME}}.exe

  test:
    desc: Run Go checks after regenerating frontend assets and bindings
    deps: [frontend, bindings]
    cmds:
      - go test ./...
